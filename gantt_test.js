'use strict';

describe('myApp.gantt.gantt_view module', function() {

  beforeEach(module('myApp.gantt.gantt_view'));

  describe('gantt_view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('GanttViewCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});
